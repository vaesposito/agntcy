# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Env
.env
.env.local

# Data — local SQLite + filesystem cache + workspace state are dev artifacts
data/

# Node
node_modules/
.next/
.next-pages/
out/
dist/
*.tsbuildinfo

# Claude Code — settings.local.json is per-developer
.claude/settings.local.json

# Helm — subchart archives are regenerated by `helm dependency update`
charts/**/charts/*.tgz

# Docker image build stamps (regenerated by make build-images)
.build/

# OS
.DS_Store
